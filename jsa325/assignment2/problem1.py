import sys
import datetime
inputDate = sys.argv[1]
lines = []

myFile = open('logAfterAssingment1.txt', 'r')

#for line in myFile:
  #lines.append(line)
#myFile.close()

#numLines = len(lines)
#for i in range(0, numLines):
  #if lines[i].find("Date:") != and i < (numLines):
    #if date_compare < datetime.datetime(year, month, day, hour, minute, second)
